# SITREP — shilp-sutra audit handoff (2026-06-30)

**From:** audit session (docs/plans only — no code changed)
**Branch at audit time:** `feat/remove-card-accent-rail`
**Your job:** implement the fixes in the two audit docs below.

## Read these first
- `docs/audit/2026-06-30-primitive-bypass-audit.md` — components hand-rolling base primitives instead of composing them.
- `docs/audit/2026-06-30-truncation-audit.md` — text-overflow handling gaps.

Both have file:line tables verified against source as of 2026-06-30. Treat them as your work list.

## ⚠️ Deconflict before touching code
- **StatCard (`ui/stat-card.tsx`) is being fixed by another session.** Do NOT touch it. That's finding A1. Check with the user / that session before editing the file.
- Watch the Card `flat` mismatch when others touch Card: **Card `flat` = no border; StatCard `flat` = border.** Don't let a Card refactor silently change StatCard's flat look.

## Bypass audit — status per finding

**Cleared to implement (decisions locked):**
- **A3** DataTableCards → compose `Card variant="outline"`. Non-breaking.
- **C1** `chat/message.tsx:348` edit textarea → compose `<Textarea>` + className for 13px.
- **C2** `schedule-send.tsx:104` → compose `Select`/`Input`.
- **D1** oauth `defaultBadge:310` → keep as exception, add `// intentional: corner pill below Badge xs` comment. Do NOT add a new Badge size.
- **E** Add micro-type tokens `--text-ds-2xs` (13px), `--text-ds-3xs` (11px), `--text-ds-4xs` (9px); replace `text-[13px]/[11px]/[9px]` magic values (message.tsx ×5, avatar, badge-indicator, oauth, color-swatch). Additive, non-breaking.
- **F** Optional guard in `pre-publish-audit.mjs` (flag Button/Card signature in non-overlay components). Your call whether to build.

**🛑 BLOCKED — do NOT implement until user rules:**
- **A2 ContentCard** (`composed/content-card.tsx` — duplicate Card CVA). Options: compose Card internally [recommended, non-breaking] / merge+deprecate [breaking] / leave.
- **Class B** (12 Button bypasses; 8 in `composed/rich-chat-input/schedule-send.tsx`). Options: refactor all [recommended; B2 AM/PM → ToggleGroup] / visible-only / defer.
- Visual explainer for these two: https://claude.ai/code/artifact/969f08eb-f47f-4256-a483-2fbda11ac786

## Truncation audit — status: decided, one PR
~25 sites + one new primitive, all together:
- **Build `<TruncatedText>`** — handles end / `line-clamp` / `mode="middle"` / overflow-aware recovery (tooltip only when `scrollWidth > clientWidth`, full string as accessible name). Subsumes a standalone `truncateMiddle`.
- Apply across: **M** (10 missing-truncation sites), **B2** (8 no-recovery), **W** (6 wrong-strategy — filenames/emails → `mode="middle"`).
- **B1** (7 `min-w-0` flex bugs): **verify each ancestor first** — some may already work via `max-w`. Add `min-w-0` only where confirmed.
- Recovery internals (bare `title` vs tooltip): user is settling with you. Audit stance: favor the overflow-aware tooltip.
- **R5 guard:** noted, your call.

**Do NOT truncate** (rule #1): page-header H1 title, toast title/body. `inline-edit.tsx:88,129` `.slice()` is a validation cap — leave it.

## Suggested order
1. Confirm StatCard ownership (avoid collision).
2. Get user ruling on A2 + Class B.
3. Truncation PR (self-contained, primitive + sites).
4. Bypass cleared items (A3, C1, C2, D1, E) ± F guard.
5. A2 + Class B once unblocked.

All changes are versioned per repo norms (changeset + Version PR). E is additive; A2-merge path would be breaking — flag in the changeset if taken.
