# Generated by post-build — copies of repo-root docs so they ship in the
# npm tarball. Source of truth is at repo root; do not edit these copies.
/MIGRATION.md
/AGENTS.md
/docs/rollback.md
/skill/

# Generated by build-mcp-manifest.mjs (post-build + version-packages). A pure
# build artifact regenerated from src + docs/components — never hand-edited, so
# it is not committed (was: 12K-line diff churn + guaranteed cross-workstream
# merge conflicts on every component change). Still shipped: it's in package.json
# `files[]` (an npm allowlist that packs it regardless of .gitignore). Freshness
# is guaranteed at the points that matter — release.yml `Build` regenerates it
# before publish, and pre-publish-audit gates it (`--check` + version-stamp).
/mcp-manifest.json
