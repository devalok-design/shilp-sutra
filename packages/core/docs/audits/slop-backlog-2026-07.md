# Slop backlog — 2026-07 (anti-slop program Phase 2)

Generated by `scripts/check-slop.mjs` over `src/{ui,composed,shell,ai}` (186 components), corpus `scripts/slop-corpus.json` v1 (6 rules). Regenerate: `node scripts/check-slop.mjs` (human) / `--json` (machine). Plan: `docs/plans/2026-07-23-anti-slop-program.md`.

## Result: 1 genuine finding across 186 components
The DS is remarkably clean by the v1 signatures. This reflects both real quality AND a young corpus — the easy-to-regex tells (side-stripe, gradient-text, sparkle, large-radius, heavy-shadow, caps-overuse) are nearly absent; the harder-to-detect tells (chip-spam, everything-a-card, eyebrow-over-heading, middle-dot cramming, filled+outline button duo) are **not yet encoded** and need smarter detection (AST / structural), not line regex.

## Open (P1)
| File:line | Rule | Note |
|---|---|---|
| `composed/date-picker/time-picker.tsx:212` | caps-overuse ×4 | Hr/Min/(Sec/AmPm) column headers in `uppercase tracking-wider`. Borderline: arguably the ONE sanctioned "column-header" caps role (§3.4) repeated, not 3 different roles. **Decision pending:** soften to mixed-case (calmer, §3.4-preferred) OR annotate as the sanctioned role. The rule's "3+ roles" intent is approximated by "3+ lines", which over-counts a single repeated role — a corpus-tuning TODO. |

## Suppressed (false positives, annotated with reason)
| File:line | Rule | Why legit |
|---|---|---|
| `composed/rich-text-editor.tsx:61` | side-stripe | Markdown blockquote left-rule — standard typography, not a card accent-rail. |
| `ui/text.tsx` | caps-overuse | The Text primitive that DEFINES the `label-*` uppercase variants — not on-surface overuse. |

## Corpus TODO (raise coverage before trusting a low count)
- Encode structural tells (need AST, not regex): everything-a-card / nested cards, filled+outline button duo, eyebrow-over-heading, chip-spam (many tinted pills), middle-dot fact-cramming.
- Tune `caps-overuse` to count distinct ROLES, not lines (dedupe repeated same-role headers).
- Consider scanning `.stories.tsx` for demo-only slop (currently excluded).
- Cross-check each new corpus rule against a known-good component to bound false positives.
